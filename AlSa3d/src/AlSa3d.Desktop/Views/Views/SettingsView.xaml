<UserControl x:Class="AlSa3d.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F5F6FA">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2E86DE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1B5F9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Background" Value="#26DE81"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1DBF6E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="إعدادات النظام" FontSize="28" FontWeight="Bold" Foreground="#2C3A47" FontFamily="Cairo"/>
                <TextBlock Text="تكوين النظام والعملات والصلاحيات" FontSize="14" Foreground="#747D8C" Margin="0,5,0,0" FontFamily="Cairo"/>
            </StackPanel>
        </Border>

        <!-- Settings Tabs -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20">
            <TabControl Background="Transparent" BorderBrush="#D1D8E0" FontFamily="Cairo">
                <TabItem Header="⚙️ الإعدادات العامة">
                    <StackPanel Margin="20">
                        <TextBlock Text="اسم الشركة" FontSize="14" Margin="0,0,0,5" FontFamily="Cairo"/>
                        <TextBox Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15" BorderBrush="#D1D8E0"/>
                        
                        <TextBlock Text="العملة الافتراضية" FontSize="14" Margin="0,0,0,5" FontFamily="Cairo"/>
                        <ComboBox SelectedValue="{Binding DefaultCurrency, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Padding="10" Margin="0,0,0,15" Width="200" HorizontalAlignment="Left">
                            <ComboBoxItem Content="EGP"/>
                            <ComboBoxItem Content="SAR"/>
                            <ComboBoxItem Content="USD"/>
                        </ComboBox>
                        
                        <TextBlock Text="رقم الهاتف" FontSize="14" Margin="0,0,0,5" FontFamily="Cairo"/>
                        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15" BorderBrush="#D1D8E0"/>
                        
                        <Button Content="💾 حفظ الإعدادات" Style="{StaticResource PrimaryButton}" 
                                Command="{Binding SaveSettingsCommand}" HorizontalAlignment="Left" Width="200"/>
                    </StackPanel>
                </TabItem>
                
                <TabItem Header="👥 إدارة المستخدمين">
                    <StackPanel Margin="20">
                        <TextBlock Text="صلاحيات المستخدمين والأدوار" FontSize="14" Margin="0,0,0,15" FontFamily="Cairo"/>
                        <Button Content="➕ إضافة مستخدم" Style="{StaticResource PrimaryButton}" 
                                Command="{Binding AddUserCommand}" HorizontalAlignment="Left" Width="200"/>
                    </StackPanel>
                </TabItem>
                
                <TabItem Header="🔒 النسخ الاحتياطي">
                    <StackPanel Margin="20">
                        <TextBlock Text="إعدادات النسخ الاحتياطي التلقائي" FontSize="14" Margin="0,0,0,15" FontFamily="Cairo"/>
                        <CheckBox Content="تفعيل النسخ الاحتياطي اليومي" IsChecked="{Binding AutoBackupEnabled}" Margin="0,0,0,15" FontFamily="Cairo"/>
                        <Button Content="📦 نسخ احتياطي الآن" Style="{StaticResource SuccessButton}" 
                                Command="{Binding CreateBackupCommand}" HorizontalAlignment="Left" Width="200"/>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>